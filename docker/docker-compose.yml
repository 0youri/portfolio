services:

  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
    restart: on-failure

  frontend:
    build:
      context: ../frontend
      dockerfile: ../docker/Dockerfile
    restart: always
    ports:
      - "3000:3000"